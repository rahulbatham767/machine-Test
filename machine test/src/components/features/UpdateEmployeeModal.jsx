import UpdateEmployee from "./UpdateEmployee";
import {
  Modal,
  ModalOverlay,
  ModalContent,
  ModalHeader,
  ModalCloseButton,
  ModalBody,
} from "@chakra-ui/react";

const UpdateEmployeeModal = ({ isOpen, onClose }) => {
  return (
    <Modal isOpen={isOpen} onClose={onClose}>
      <ModalOverlay />
      <ModalContent>
        <ModalCloseButton />
        <ModalBody>
          <UpdateEmployee />
        </ModalBody>
      </ModalContent>
    </Modal>
  );
};
export default UpdateEmployeeModal;
